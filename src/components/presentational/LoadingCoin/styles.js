import { StyleSheet } from 'react-native';
import Constants from 'config/constants/Constants';

export const styles = StyleSheet.create({
  spiningCoinContainer:{
    justifyContent:'center',
    alignItems:'center',
    alignSelf:'center'
  },
});
